# 全能小说作家（多 Agent 版）

[![Version](https://img.shields.io/badge/version-9.5.10-blue)](https://github.com/Fly143/ultimate-novel-creation-skill)
[![License](https://img.shields.io/badge/license-MIT-green)](LICENSE)

面向 AI Agent 的全流程长篇小说创作技能。从书名生成到千章完稿，配备三大自动系统（记忆系统/强约束/叙事质量管理）确保长篇连贯性。

> **本分支（`agents-split`）= 多 Agent 拆分版**：将技能拆为 **5 个持久 Agent**（主编/写手/审核官/修复师/设定师），各自独立运行、流水线协作，写审分离铁律在 Agent 边界上强制生效。
> **`main` 分支 = 单体版**：单 Agent 内部通过子代理完成全流程（功能等价）。

---

## 🚀 快速开始（多 Agent 群聊模式）

### 一、准备 5 个 Agent（每个 Agent = 一个 Hermes profile）

| 角色 | 目录 | 职责 | 对应原功能模块 |
|---|---|---|---|
| 🧭 主编 | [`agents/00_主编_director/`](agents/00_主编_director/SKILL.md) | 唯一调度者：意图识别/场景路由/流水线编排 | 00 协议路由、05_26 步骤1/5 |
| ✍️ 写手 | [`agents/01_写手_writer/`](agents/01_写手_writer/SKILL.md) | 唯一正文生产者 + 记忆归档者 | 05_05~11、05_26 步骤2 |
| 🔍 审核官 | [`agents/02_审核官_auditor/`](agents/02_审核官_auditor/SKILL.md) | 唯一审核者：八项全量审核 + 记忆↔正文双向对账 | 05_12/13/14/17/22/24/25 |
| 🔧 修复师 | [`agents/03_修复师_fixer/`](agents/03_修复师_fixer/SKILL.md) | 唯一改稿者 + 记忆修复者 | 05_16、05_26 步骤4、18F |
| 📋 设定师 | [`agents/04_设定师_setter/`](agents/04_设定师_setter/SKILL.md) | 设定唯一权威：写作前设定 + 手动编辑圣经 | 01/02/03/04/18C/20/27 |

每个角色目录含两份文件：
- **`SKILL.md`** — Hermes 内部使用，作为该 Agent 的技能加载
- **`system_prompt.md`** — 第三方宿主（Claude Code / Cursor / API 接入 / Dify·Coze）使用，整段复制到 system prompt 位置

### 二、部署步骤（Hermes 桌面端 Bot Mode）

**推荐：桌面端 UI 直接建 agent（无需手动建 profile）**

1. **新增 agent**：桌面 app → Bot Mode 面板 → 在当前实例中「新增 agent」，为 5 个角色各建一个（profile 目录名任意，如 `director`/`writer`/`auditor`/`fixer`/`setter` 或 `novel` 系均可）。底层自动完成：创建独立 profile + 写入 Bot 标识（`ui_meta.hermes-bots`，含样式与 canonical 会话）+ 克隆当前实例配置（模型/cwd）。脚本按目录名自动识别角色，无需固定命名。
2. **换 SOUL**：把每个 agent 的 `SOUL.md` 替换为对应角色 `system_prompt.md` 的内容（角色边界 / 铁律 / 交接契约）。
3. **挂技能**：将本仓库复制为该 profile 的 `skills/ultimate-novel-creation-skill`（或只挂该角色需要的模块，见[模块归属映射表](agents/模块归属映射表.md)）。
4. **拉群协作**：Bot Mode 中创建群聊，把 5 个 agent 拉入。用户与主编对话，主编按序 `@写手 → @审核官 → (@修复师) → 复审 → .done`。

**备选：CLI 建 profile**

```bash
hermes profile create writer --description "小说写手"   # 目录名任意，sync 脚本按名自动识别角色
```

然后同样替换 SOUL.md、同步技能；`ui_meta.hermes-bots` 块由桌面端 Bot Mode 识别后自动补全。

> **机制说明**：桌面端「新增 agent」= 新建一个 profile 并启动独立 backend 进程（`hermes --profile <name> serve`）。每个 agent 独立持有 memory / skills / 会话历史，`ui_meta.hermes-bots` 块（shape/color/chat）是 Bot Mode 的识别与显示标识，canonical 「Bot Chat」会话承载 agent 间互 @ 与交接。

### 三、技能副本与同步（重要）

**技能不共用**：Hermes 的 profile 隔离是目录级的——每个 agent 持有 `skills/ultimate-novel-creation-skill/` 的**独立完整副本**（模块/references/templates/memory-system 全在内）。因此：
- 各 agent 物理上能读全部 27 个模块，**行为边界由 SOUL 铁律约束**（角色 SKILL.md 的专属模块清单 + 不越权条款）
- 更新技能时**不要手动逐个改**，用同步脚本一键分发（**技能 + SOUL 一起同步**）：

```bash
bash sync_agents.sh              # 同步到全部已识别的 profile（按目录名自动识别角色）
bash sync_agents.sh writer        # 只同步指定 profile（须存在于 profiles 根目录）
bash sync_agents.sh --dry-run     # 先预览将同步哪些 profile
# 角色无法自动识别时可用 <profile>=<角色目录> 强制映射，例：
bash sync_agents.sh writer=01_写手_writer
```

脚本将仓库（唯一事实源）整体覆盖到各 profile 的技能目录，并**从 `agents/<角色>/system_prompt.md` 重新生成各 profile 的 `SOUL.md`**（角色 SOUL 的权威来源是仓库，本地不要手改 SOUL——改了也会被脚本覆盖）。**改技能/人格的正确姿势**：先在仓库改（SKILL.md / system_prompt.md）→ commit/push → 跑 `sync_agents.sh` 分发到各 agent。

> **人格说明**：主编（任意实例名，如 `director` 或 `novel`）默认融合「小爱同学」创作导师人格（十年网文经验资深小说家），定义在 `agents/00_主编_director/system_prompt.md`，部署时自动进入 SOUL。不需要可删减该节。

### 四、流水线时序（场景B 继续创作）

```
主编 ──章节指令+细纲──▶ 写手
写手 ──正文+归档确认──▶ 主编
主编 ──正文路径───────▶ 审核官
审核官 ──审核报告(PASS/FAIL)──▶ 主编
   ├─ 全PASS → 主编写 .done 闭环
   └─ 有FAIL → 主编 ──FAIL报告+正文──▶ 修复师
                修复师 ──修复后正文──▶ 主编 ──▶ 审核官 复审
                复审PASS → 主编写 .done 闭环
```

### 五、核心铁律（所有 Agent 必须遵守）

1. **写审分离**：写手不审核自己产出，审核官不改稿，修复师只按报告改——三方必须不同 Agent。
2. **显式顺序呼叫**：主编必须显式 @ 下一个 Agent 并按序推进，不依赖群聊自发发言。
3. **记忆三权分立**：每章记忆归档（圣经五件套+摘要）= 写手；手动编辑圣经（18C）= 设定师；修复跑偏记忆（18F）= 修复师。其他 Agent 不得直接改写圣经文件。
4. **双向对账**：审核官发现记忆与正文不一致时，按溯源规则判责（新章vs归档→归档错；新章vs历史→翻原文；新章vs设定→查约束源），不默认正文错。
5. **不越权**：各 Agent 只执行自己职责清单内的工作，其余一律交还主编路由。

### 六、模块归属

27 个功能模块的归属（🟢专属 / 🟡共享 / 🔵主编调度）见 [`agents/模块归属映射表.md`](agents/模块归属映射表.md)。模块文件**不物理复制**，统一存放根目录 `modules/`（单一事实源）。

---

# 单体模式（main 分支用法）

> 单 Agent 部署：将本仓库作为技能加载，Agent 内部通过 `delegate_task` 子代理完成 写 → 审 → 修 闭环（功能等价于多 Agent 版）。

## ⚠️ 使用前必读（先看这里）

本技能是**内容包**：加载后是否被严格执行，取决于宿主 agent 的 system prompt。**直接把仓库加载为技能就开写，agent 很可能会自由发挥、跳过流程，体验大打折扣。**请按以下三步使用：

1. **配置 system prompt**：将仓库根目录的 [system_prompt.md](system_prompt.md) 整段复制，配置为宿主软件的 system prompt（Claude Code / Cursor / API 接入 / Dify·Coze 等均适用，该文件顶部有各平台用法说明）
2. **加载技能**：将本仓库作为技能/上下文加载
3. **开始创作**：说「写小说」「继续」或任一触发词，技能会按场景路由引导

> 所有流程约束（强制执行协议、写后流水线、记忆系统维护等）都以技能文件为准；system prompt 只负责让宿主 agent 强制遵守它们。

## 三大场景入口

| 场景 | 什么时候用 | 触发方式 |
|:----:|-----------|:--------:|
| **A. 新开小说** | 从零开始构思一本新书 | 说 `写小说` / `新开一本` |
| **B. 继续创作** | 已有进行中的小说接着写 | 说 `继续` / `接着写` / `继续写《XXX》` |
| **C. 单个功能** | 只需某个特定功能（含记忆系统工作流） | 直接说功能名（见下方） |

---

## 27 大功能模块总表

### 🎯 立项类

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **1** | **项目初始化** | 生成书名、创建立项定位单、选择文风（通用/指定作者/指定作品）、推荐创作模式 | `书名` / `立项` / `初始化` |
| **2** | **世界观构建** | 设定世界规则、力量体系、社会结构 | `世界观` / `世界设定` |
| **3** | **人物设计** | 创建人设卡、人物关系图谱 | `人设` / `人物` / `角色` |
| **4** | **情节大纲** | 生成故事主线、章节规划表、百万字规划 | `大纲` / `情节` / `章节规划` |

### ✍️ 创作类

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **5** | **黄金开篇创作** | 生成前三章黄金开篇，抓住读者 | `开篇` / `黄金开篇` / `开头` |
| **6** | **单章写作** | 🔥 **核心功能**，生成单章正文并自动维护记忆系统 | `写第X章` / `下一章` / `继续` |
| **7** | **爽点设计** | 设计打脸爽、升级爽、装逼爽等桥段 | `爽点` / `爽点设计` |
| **8** | **冲突与反转** | 设计剧情冲突、反转情节、高潮铺垫 | `冲突` / `反转` |
| **9** | **对话场景** | 生成对话为主的场景，突出角色性格 | `对话` / `对话场景` |
| **10** | **打斗场景** | 生成战斗、对决等打斗场景 | `打斗` / `战斗` / `对决` |
| **11** | **AI/系统流** | AI 觉醒、系统面板、数据流等特殊场景 | `系统流` / `AI流` |

### 🔍 诊断类

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **12** | **节奏诊断** | 分析章节节奏过快/过慢/混乱，给出优化建议 | `诊断` / `节奏` / `节奏诊断` |
| **13** | **章节质量检查** | 检查文笔、逻辑、结构等质量维度 | `质量检查` / `检查章节` |
| **14** | **创新深度检查** | 检查创新点、差异化程度 | `创新检查` / `差异化` |
| **15** | **商业评估** | 评估作品市场潜力、对标竞品分析 | `商业评估` / `市场分析` |
| **16** | **修订优化** | 修改已有章节、润色文笔、调整内容 | `修订` / `修改` / `润色` |
| **17** | **连贯性检查** | 检查前后情节衔接、逻辑自洽、伏笔呼应 | `连贯性` / `一致性` |

### 🧠 长篇管理

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **18** | **记忆系统管理** | 7 大工作流：初始化/查看/编辑/压缩/恢复/修复（详见下方） | `记忆系统` / `看记忆` / `检查状态` |

### 🛠️ 工具类

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **19** | **导出功能** | 导出为网文 txt（合并单文件，可指定章节范围） | `导出` / `导出txt` |
| **20** | **批量设定** | 批量生成配角人设、支线剧情、反派角色等设定内容 | `批量` / `批量生成` / `批量人设` |
| **21** | **版本控制** | 记录修改历史，支持回滚对比 | `版本` / `回滚` / `历史` |

### 🔒 约束类

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **22** | **强约束系统** | 金手指叙事约束 / 时间约束 / 多条叙事线监控，每章自动检查 | `金手指` / `时间约束` / `叙事线` / `强约束` |

### 📖 分析类

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **23** | **作品拆解分析** | 人物/风格拆解 + 全书拆文库（情节节点/情绪模块/人物模块/文风素材库），知识库+原文双模式 | `分析《XXX》的YYY` / `拆解《XXX》` / `分析《XXX》的风格` |

### ⚙️ 质量管控

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **24** | **叙事质量管理框架** | 写前预检 / 写后三阶复核 / YAML参数链条存档 / 可替换规则集 | `预检` / `复核` / `规则集` |
| **25** | **读者审稿** | 单章4维度评分（连贯/吸引/爽点/bug），挑剔读者视角审核 | `读者审稿` / `审稿` / `审核剧情单元` |
| **26** | **写后流水线（模块化）** | 每章必跑闭环：字数→圣经×5+摘要→1个全量审核子代理 U（合并五道门禁+强约束+逻辑因果+读者审稿+质量+连贯性+节奏+叙事规则八项）→修复→.done | 自动触发（「继续」写完后自动执行） |

### 📥 导入类

| 编号 | 功能名称 | 能做什么 | 🗣️ 触发命令 |
|:----:|:---------|:---------|:-----------|
| **27** | **导入已有小说** | 把已有稿子（半成品/完本）反向解析为标准项目（正文分章+记忆系统+设定），可直接续写 | `导入` / `导入《XXX》` / `导入小说` |

---

## 记忆系统 7 大工作流（功能 18）

> A / D / D2 / E 由系统自动触发。B / C / F 可手动触发，在「单个功能」模式下直接说触发命令即可。

| 工作流 | 名称 | 方式 | 能做什么 | 🗣️ 触发命令 |
|:------:|:----|:----:|:---------|:-----------|
| **A** | **初始化** | 自动 | 新建小说时自动创建记忆库，写入设定 | （项目初始化时自动执行） |
| **B** | **查看状态** | 手动 | 看进度、人物状态、伏笔待回收 | `看记忆` / `检查状态` / `显示伏笔` |
| **C** | **手动编辑** | 手动 | 更新人设变化、标记伏笔已回收 | `更新人设` / `改伏笔` / `编辑圣经` |
| **D** | **阶段压缩**（每10章） | 自动 | 压缩记忆，保持上下文紧凑 | （每10章自动执行） |
| **D2** | **卷压缩**（每50章） | 自动 | 更大粒度压缩 + 刷新所有伏笔 | （每50章自动执行） |
| **E** | **跨会话恢复** | 自动 | 🔥 **关闭后回来秒恢复** | 说 `继续写《XXX》` 自动触发 |
| **F** | **紧急修复跑偏** | 手动 | 检查 OOC、剧情矛盾、伏笔对不上 | `人物跑偏了` / `检查一致性` / `修复记忆` |

---

## 自动触发的机制（不用手动叫）

这些是系统**自动执行**的，写一章跑一套：

| 触发时机 | 自动做的事 |
|:---------|:-----------|
| **每章生成后** | ✅ 写后流水线（03_26）自动闭环：字数确认→圣经×5更新+摘要→1个全量审核子代理 U（合并五道门禁+强约束+逻辑因果+读者审稿+质量+连贯性+节奏+叙事规则八项）→修复→.done标记 + **YAML 参数链条存档**（03_26 步骤6） |
| **「继续」命令** | ✅ 写前预检（5 字段状态核对，缺字段阻断 → 自动回溯修复记忆系统 → 重新预检通过） |
| **每5章** | ✅ 连贯性深度检查 + 质量检查深度模式 |
| **每10章** | ✅ 自动阶段压缩 + 创新深度检查 + 参数链压缩（阶段N_参数.md） |
| **每50章** | ✅ 自动卷压缩 + 参数链压缩（卷N_参数.md） |
| **开篇章节** | ✅ 自动匹配黄金开篇参考资源 |
| **战斗章节** | ✅ 自动加载打斗场景创作指南 |
| **爽点章节** | ✅ 自动加载爽点设计策略 |

---

## 快捷命令速查卡

| 您这样说 | 直接跳到 |
|:---------|:---------|
| `写小说` / `新开一本` | 场景 A → 选创作模式 |
| `继续` / `下一章` | 场景 B → 单章写作 |
| `继续写《XXX》` | 场景 B + 跨会话恢复 |
| `世界设定` / `世界观` | 功能 2 世界观构建 |
| `书名` | 功能 1 项目初始化 |
| `人设` / `人物` / `角色` | 功能 3 人物设计 |
| `大纲` | 功能 4 情节大纲 |
| `开篇` / `黄金开头` | 功能 5 黄金开篇 |
| `诊断` / `节奏` | 功能 12 节奏诊断 |
| `爽点` | 功能 7 爽点设计 |
| `战斗` / `打斗` | 功能 10 打斗场景 |
| `看记忆` / `检查状态` | 功能 18 工作流 B |
| `人物跑偏了` | 功能 18 工作流 F |
| `导出` | 功能 19 导出 |
| `金手指` / `叙事线` / `时间约束` | 功能 22 强约束系统 |
| `分析《XXX》的YYY` | 功能 23 人物拆解 |
| `分析《XXX》的风格` | 功能 23 风格拆解 |
| `写后` / `收尾` | 执行完整写后流水线（03_26） |
| `半程收尾` | 快速模式：仅圣经更新+摘要+字数 |
| `补缺` | 按 .done 标记补齐遗漏步骤 |
| `读者审稿` / `审稿` | 功能 25 读者审稿 |
| `预检` | 功能 24 写前预检 |
| `复核` | 功能 24 写后复核 |
| `规则集` | 功能 24 规则集管理 |
| `批量` | 功能 20 批量设定 |
| `冲突` / `反转` | 功能 8 冲突反转设计 |
| `对话` / `对话场景` | 功能 9 对话场景 |
| `系统流` / `AI流` | 功能 11 AI/系统流创作 |
| `修订` / `修改` / `润色` | 功能 16 修订优化 |
| `商业评估` / `市场分析` | 功能 15 商业评估 |
| `菜单` / `帮助` | 显示本手册 |
| `导入` / `导入《XXX》` | 功能 27 导入已有小说 |
| `切书` / `换书` / `列出我的书` | 多书切换（.active-book） |

---

## 文件产出结构

每写一本书，系统会自动创建这个目录：

```
[书名]/
├── 设定/
│   ├── [书名]_立项定位单.md
│   ├── [书名]_文风设定.md
│   ├── [书名]_世界观设定.md
│   ├── [书名]_人设卡.md
│   ├── [书名]_人物关系图谱.md
│   ├── [书名]_情节大纲.md
│   ├── [书名]_分卷大纲.md
│   ├── [书名]_章节规划表.md
│   ├── [书名]_角色储备表.md
│   ├── 细纲/
│   │   └── 第NNN章_细纲.md
│   ├── 拆解/
│   │   └── {作品名}/          ← 拆文库（功能 23：拆解报告/情节节点/情绪模块/人物模块/文风）
│   ├── 创作状态追踪表.md
│   └── 进度看板.md
├── 报告/                        ← 审核/评估/修订类报告（功能12-17/25/26 产出）
│   ├── 第NNN章_全量审核报告.md  ← 每章流水线生成（U 八项 + 修复记录）
│   ├── 商业可行性评估报告.md
│   └── 创新深度检查报告.md
├── 记忆系统/
│   ├── 圣经/
│   │   ├── 故事圣经.md
│   │   ├── 人物弧线.md
│   │   ├── 剧情时间线.md
│   │   ├── 伏笔清单.md
│   │   ├── 角色数据库.md
│   ├── 约束/
│   │   ├── 金手指约束.md
│   │   ├── 时间约束.md
│   │   └── 叙事线.md
│   ├── 摘要/
│   │   ├── 第001章摘要.md ...
│   ├── 阶段/
│   │   └── 阶段1_第1-10章.md ...
│   └── 卷/
│       └── 卷1_第1-50章.md ...
└── 正文/
    ├── 第001章_章节名.txt
    └── 第002章_章节名.txt ...
```

---

## 项目结构（本技能仓库）

```
├── SKILL.md              # 技能入口 + 路径约定
├── agents/               # 多 Agent 拆分版（本分支）
│   ├── 模块归属映射表.md  # 27 模块归属（专属/共享/主编调度）
│   ├── 00_主编_director/  # 🧭 主编（SKILL.md + system_prompt.md）
│   ├── 01_写手_writer/    # ✍️ 写手
│   ├── 02_审核官_auditor/ # 🔍 审核官
│   ├── 03_修复师_fixer/   # 🔧 修复师
│   └── 04_设定师_setter/  # 📋 设定师（总览与部署说明见本文件上文）
├── modules/              # 32 个模块文件（27 功能模块 + 5 核心协议/规范）
├── references/           # 11+2 个精选写作参考（含 rulesets/ 子目录）
├── templates/            # 13+3 个输出模板（含 constraints/ 子目录）
└── memory-system/        # 记忆系统模板（skill 内只读）
    ├── bible/            # 故事圣经 / 人物弧线 / 剧情时间线 / 伏笔清单 / 角色数据库
    ├── summaries/        # 单章摘要模板
    ├── phases/           # 阶段压缩模板（每 10 章）
    └── volumes/          # 卷压缩模板（每 50 章）
```

## 核心能力

- **27 个功能模块**：项目初始化 → 世界观 → 人物 → 大纲 → 单章写作 → 爽点/冲突/对话/打斗 → 节奏诊断 → 质量检查 → 强约束系统 → 作品拆解分析 → 叙事质量管理 → 读者审稿 → 写后流水线 → 导入已有小说 → 导出发布
- **五层自然语言记忆系统**：故事圣经 / 人物弧线 / 剧情时间线 / 伏笔清单 / 角色数据库，LLM 直读直写，不依赖外部工具
- **强约束系统**：金手指叙事约束 / 时间约束 / 多条叙事线监控，叙事参谋风格，不报违规只给建议
- **叙事质量管理框架**：写前预检 / 写后三阶复核 / YAML参数链条存档 / 可替换规则集，全流程叙事品控
- **千章级自适应上下文**：章节类型判定（高潮/伏笔回收/过渡/标准），自动调配加载深度，1000 章不爆上下文
- **远古伏笔语境回读**：第 500 章回收第 1 章伏笔时，自动回读原始章节摘要保证语境完整
- **Bible 文档增长控制**：超 30KB 自动压缩远古内容，细节下沉到卷压缩，按需回读
- **五道生成门禁**：引用校验/一致性校验/蓝图出场校验/未知实体检测/描写一致性，不通过不视为完成
- **风格质量检查**：语言风格统一性/叙述视角一致性/文风成熟度（03_13，并入全量审核子代理 U 的 F 项）
- **11+2 个精选写作参考**：爽文理论、爽点设计、黄金开篇、风格技巧、打斗场景、高级写作技法、规则集系统（通用默认+赛博-技术流，支持自定义）
- **13+3 个精选模板**：立项定位单、文风设定、世界观、人设卡、人物关系、情节大纲、章节规划、单章结构、质量检查、约束系统（金手指/时间/叙事线）等

## 快速开始（单体模式）

将本仓库作为 AI Agent 的技能加载。Agent 加载后自动读取 `SKILL.md` 及相关模块。

用户输入「写小说」即可触发完整创作流程。长篇连载自动启用记忆系统。

在第三方软件（Claude Code / Cursor / API 接入 / Dify·Coze 等）中使用时，可将 `system_prompt.md` 配置为该软件的 system prompt，确保宿主 agent 严格按技能执行、不自由发挥。

## 旧项目迁移（v9.4.0 之前）

9.4.0 起记忆系统产出路径中文化（bible→圣经、summaries→摘要、phases→阶段、volumes→卷、constraints→约束）。9.4.0 之前创建的项目需迁移，否则跨会话恢复找不到项目：

```powershell
# 在项目目录（含 [书名]/ 的上级目录）运行
powershell -ExecutionPolicy Bypass -File scripts/migrate_memory_paths.ps1          # 预览
powershell -ExecutionPolicy Bypass -File scripts/migrate_memory_paths.ps1 -Apply   # 执行
```

脚本已随各 release 附于 Assets（`migrate_memory_paths.ps1`）。

## 声明

本技能所有内容均由 AI 生成，不含任何人工编写，望周知！

---
name: 全能小说作家
description: 全流程长篇小说创作技能，28个功能模块，支持 AI 全自动和分步引导双模式。涵盖书名生成、人设构建、大纲规划、章节写作、爽点设计、节奏诊断、风格创作、强约束系统、作品拆解分析（含拆文库）、叙事质量管理、导入已有小说、轻量直出短文、朱雀去AI化与「人工为主」验收、写后流水线硬门禁。配备三大自动系统：自然语言记忆系统（故事圣经/人物弧线/剧情时间线/伏笔清单/角色数据库）、强约束系统（金手指叙事/时间约束/叙事线监控）、叙事质量管理框架（写前预检/三阶复核/参数链条）。触发词：「写小说」「新开一本」「继续」「接着写」「下一章」「书名」「人设」「大纲」「看记忆」「导出」「导入」「切书」「金手指」「叙事线」「分析《XXX》」「菜单」「预检」「复核」「规则集」「疑似AI」「去AI化」「朱雀」「写后」「补缺」。
user-invocable: true
tags: [小说, 创作, 写作, 长篇, 故事, 文学, 大纲, 人物, 章节写作, 爽文, 网文, 平台文案, 书名生成, 记忆系统, 自然语言记忆, 风格创作, 短篇爆款, 朱雀, 去AI化, 疑似AI, AIGC检测, 人工为主]
version: 9.6.0
---

## 📂 文件路径约定

**本 skill 中所有文件引用遵循以下规则：**

1. **skill 内部文件**：`modules/`、`references/`、`templates/`、`memory-system/`（skill 内模板）等路径，均相对于本 skill 的根目录。
   - 示例：`references/书名生成策略.md` → skill 目录下的 references/书名生成策略.md
   - 示例：`memory-system/bible/story_bible.md`（不带 `[书名]/` 前缀）→ skill 内模板文件，只读

2. **项目文件**：带 `[书名]/` 前缀的路径，相对于当前工作目录。`[书名]` 是项目初始化时用户确认的书名。
   - 示例：`[书名]/记忆系统/圣经/故事圣经.md` → 项目目录下的记忆系统副本
   - 示例：`[书名]/设定/[书名]_人设卡.md` → 项目目录下的设定文件

3. **记忆系统文件区分**：
   - `memory-system/xxx`（无前缀）= skill 内的只读模板 → 仅用于项目初始化时复制
   - `[书名]/记忆系统/xxx`（有前缀）= 项目目录下的读写副本 → 所有后续操作的目标
   - 中英对应示例：`memory-system/bible/story_bible.md`（模板）→ 初始化复制为 `[书名]/记忆系统/圣经/故事圣经.md`（项目副本）；`memory-system/summaries/_template.md` → `[书名]/记忆系统/摘要/模板.md`

4. **项目查找**：跨会话恢复时，在当前工作目录下搜索 `*/记忆系统/圣经/故事圣经.md` 定位已有项目。

# 全能小说作家

---

## ⛔ 强制执行协议

> **本技能采用模块化架构，加载后必须先读取 `modules/00_强制执行协议.md`。**
>
> 00 协议包含完整的：意图识别、场景路由、写作/写后命令、快捷命令映射、写前检查点、
> 操作确认点、边界条件。
> **除下方★「章节完成硬定义」为防跳步内联副本外，本文件其余协议不重复，以 00 协议为唯一权威。**
>
> **启动路径唯一**：加载后只读根 `SKILL.md` → `modules/00_强制执行协议.md` 一跳，无二级协议跳转。
> **权威链条**：步骤/顺序权威 = `modules/03_26_功能模块.md`；协议/命令权威 = `modules/00_强制执行协议.md`；下方★为只读硬副本。

---

## ★ 章节完成硬定义（内联，防止只加载 SKILL 时仍跳过写后）

**「本章完成」当且仅当（`.done` 为唯一权威；顺序权威 = `modules/03_26_功能模块.md` 步骤4）：**

1. 正文已保存（仅标题行 + 正文）  
2. 字数检查通过 → `.done/第NNN章_wordcount.done`  
3. 写后流水线 `modules/03_26_功能模块.md` **按序**已执行（不得「先交稿后补」）  
4. **U 审核后先跑朱雀 4.1b–4.1e → `.done_zhuque`；再处理 FAIL/建议修复**（禁止整段重写人工段；修复改动大则补跑 4.1e）  
5. 圣经/摘要 + U 相关 `.done` + **`.done/第NNN章_zhuque.done` 存在**  
6. 闭环必需 `.done` 齐全（含 `zhuque` + `merge`；10 倍数章含 `innovation`）→ 才可写 `.done_chapter`  

**结束本章时必须输出【第NNN章状态】回执**（正文/字数/写后是否执行/朱雀预检三态/人工证据包动作/.done 清单/能否写下一章）。

**漏步自愈（检测到即补跑，不询问）**：缺任一必需 `.done`（尤其 `zhuque`）→ 禁止称完成、禁止开写 N+1；对当前章自动「补缺」。`delegate_task` 失败时降级自审 **仍必须**跑 4.1b–4.1e。

**朱雀目标 = 人工为主**：三态中「人工创作特征」必须 **>**「疑似AI创作特征」。仍疑似 ≥ 人工时禁用「全文再润一遍」与同义补丁；按 `templates/朱雀去AI化检查清单.md`「人工为主协议」执行人工段注入 + 整段重生 + 结构破坏 + 强化轮。触发词：「疑似AI」「去AI化」「朱雀」。

**语义边界 / 双验收**：
- **流水线闭环** = 必需 `.done` 齐（含 `zhuque`）+ 状态回执——脚本可验。
- **朱雀检测达标** = 用户回传三态「人工 > 疑似」——以检测器+用户为准；**`.done_zhuque` 存在 ≠ 检测已通过**。
- 纯 AI 全自动常停在「疑似」；要稳定过检测通常需真人重写关键句。禁止把闭环说成过关。
- **对外汇报硬约定**：回执分列（a）流水线/check-done 结果（b）朱雀三态或「待检测」；禁止只贴 `.done` 或 exit0 冒充检测通过。
- 客观核验可选 `scripts/check-done.ps1` / `scripts/check-done.py`（**只认空标记 size=0，非空无效**；报告「人工证据包」节**结构软校验**：标题 + 结构证据类别 ≥2 类且至少含一类强证据——强证据须带段号/字数结构（如「人工段注入：第3段，约320字」），**裸抄规格词表/策略话术不算**；`-StrictReport`/`--strict-report` 可升硬失败）。**`--allow-non-empty`/`-AllowNonEmpty` 仅限用户本地调试；宿主/LLM 做闭环核验或对外宣称时禁止携带。**

---

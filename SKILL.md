---
name: 全能小说作家
description: 全流程长篇小说创作技能。涵盖书名生成、人设构建、大纲规划、章节写作、爽点设计、节奏诊断、风格创作，配备自然语言记忆系统（故事圣经/人物弧线/伏笔清单）确保长篇连贯性。支持 AI 全自动和分步引导双模式。
tags: [小说, 创作, 写作, 长篇, 故事, 文学, 大纲, 人物, 章节写作, 爽文, 网文, 平台文案, 书名生成, 记忆系统, 自然语言记忆, 风格创作, 短篇爆款]
version: 5.0.0
---




## 📂 文件路径约定

**本 skill 中所有文件引用遵循以下规则：**

1. **skill 内部文件**：`modules/`、`references/`、`templates/`、`memory-system/`（skill 内模板）等路径，均相对于本 skill 的根目录。
   - 示例：`references/书名生成策略.md` → skill 目录下的 references/书名生成策略.md
   - 示例：`记忆系统/bible/story_bible.md`（不带 `[书名]/` 前缀）→ skill 内模板文件，只读

2. **项目文件**：带 `[书名]/` 前缀的路径，相对于当前工作目录。`[书名]` 是项目初始化时用户确认的书名。
   - 示例：`[书名]/记忆系统/bible/story_bible.md` → 项目目录下的记忆系统副本
   - 示例：`[书名]/设定/[书名]_人设卡.md` → 项目目录下的设定文件

3. **记忆系统文件区分**：
   - `memory-system/xxx`（无前缀）= skill 内的只读模板 → 仅用于项目初始化时复制
   - `[书名]/记忆系统/xxx`（有前缀）= 项目目录下的读写副本 → 所有后续操作的目标

4. **项目查找**：跨会话恢复时，在当前工作目录下搜索 `*/记忆系统/bible/story_bible.md` 定位已有项目。

# 全能小说作家

---

## ⛔ 强制执行协议

> **本技能采用模块化架构，关键内容存储在 `modules/` 目录下。** 
> 
> **技能加载后，必须先读取 `modules/00_强制执行协议.md`，严格执行其中的协议要求。**

**【首次交互必须执行】**

当用户触发本技能时，执行智能识别流程：
1. 读取 `modules/00_强制执行协议.md`（含智能启动）
2. 根据用户意图直接进入对应场景，或显示简化引导
3. 等待用户选择或提供信息

**快捷命令**：
- "继续" / "下一章" → 直接进入单章写作
- "书名" / "人设" / "大纲" → 直接调用对应功能
- "菜单" / "帮助" → 显示完整功能列表

---

## 📦 模块架构索引

本技能由以下模块组成，按需加载：

| 模块文件 | 内容 | 加载时机 |
|----------|------|----------|
| `modules/00_强制执行协议.md` | 执行协议（含智能启动） | 技能启动时 |
| `modules/00_智能启动协议.md` | 意图识别、快捷入口 | 技能启动时 |
| `modules/01_启动引导.md` | 场景A/B/C引导话术 | 降级时使用 |
| `modules/02_文档存储规范.md` | 文件命名、目录结构 | 创建文件时 |
| `modules/03_实时状态追踪.md` | 追踪表、连贯性检查 | 章节生成后 |
| `modules/04_功能模块索引.md` | 22个功能模块总览 | 选择功能时 |
| `modules/05_01_功能模块.md` ~ `05_19_功能模块.md` | 功能1-19详细流程 | 执行对应功能时 |
| `modules/05_20_导出功能.md` ~ `05_22_版本控制.md` | 功能20-22详细流程 | 执行对应功能时 |
| `modules/06_场景资源映射.md` | 场景预判、资源加载 | 章节写作前 |
| `记忆系统/` | 故事圣经/人物弧线/时间线/伏笔 | 项目全周期 |

---

## 📋 功能模块列表

### 核心功能（1-19）
1. 项目初始化 | 2. 世界观构建 | 3. 人物设计
4. 情节大纲 | 5. 黄金开篇 | 6. 单章写作
7. 爽点设计 | 8. 冲突反转 | 9. 对话场景
10. 打斗场景 | 11. AI/系统流 | 12. 节奏诊断
13. 质量检查 | 14. 创新检查 | 15. 商业评估
16. 修订优化 | 17. 连贯性检查 | 18. 记忆系统
19. 特色风格

### 新增功能（20-22）
20. **导出功能** - 网文/epub/Word格式导出
21. **批量处理** - 一次生成多章
22. **版本控制** - 修改历史、回滚对比

---


## 🚀 快速加载指引

### 场景 A：新开小说
```
用户选择 A → 选择创作模式 → 执行对应功能模块
```

### 场景 B：继续创作
```
用户选择 B → 读取已有设定文件 → 加载单章写作 → 继续创作
```

### 场景 C：使用单个功能
```
用户选择 C → 用户选择功能编号 → 加载对应功能模块
```

### 智能识别（推荐）
```
用户输入 → 意图识别 → 直接进入对应场景
```

---

## 重要说明

**记忆系统使用方式（v5.0 全新自然语言记忆）：**
- **记忆系统**：采用 LLM 原生可读/可写的自然语言文档（故事圣经/人物弧线/剧情时间线/伏笔清单）
- **结构**：`记忆系统/bible/`（4个故事圣经文档）+ `summaries/`（单章摘要）+ `phases/`（10章压缩块）
- **初始化**：项目初始化时自动创建记忆系统
- **自动维护**：每章写完后自动更新圣经 + 生成摘要
- **阶段压缩**：每10章自动将摘要压缩为梗概，确保跨会话记忆不丢失
- **跨会话恢复**：新会话读取圣经 + 压缩块即可秒回状态
- **手动管理**：功能18可查看/编辑/修复记忆系统

**特色风格使用方式：**
- 风格选择：项目初始化或单章写作时选择
- 风格资源：功能19提供三种风格参考
- 风格应用：创作时自动加载对应资源

**导出功能：**
- 网文格式：纯txt，适配各平台
- 电子书：epub格式，支持目录导航
- 投稿版：Word格式，标准投稿排版

---
name: 全能小说作家
description: |
  全流程长篇小说创作技能，24个功能模块，支持 AI 全自动和分步引导双模式。
  涵盖：书名生成、人设构建、大纲规划、章节写作、爽点设计、节奏诊断、
  风格创作、强约束系统、作品拆解分析、叙事质量管理。
  配备三大自动系统：自然语言记忆系统（故事圣经/人物弧线/伏笔清单）、
  强约束系统（金手指叙事/时间约束/叙事线监控）、叙事质量管理框架（写前预检/三阶复核/参数链条）。
  
  触发词：「写小说」「新开一本」「继续」「接着写」「下一章」「书名」「人设」「大纲」
  「看记忆」「导出」「金手指」「叙事线」「分析《XXX》」「菜单」
  「预检」「复核」「规则集」
user-invocable: true
tags: [小说, 创作, 写作, 长篇, 故事, 文学, 大纲, 人物, 章节写作, 爽文, 网文, 平台文案, 书名生成, 记忆系统, 自然语言记忆, 风格创作, 短篇爆款]
version: 6.9.2
---


## 📂 文件路径约定

**本 skill 中所有文件引用遵循以下规则：**

1. **skill 内部文件**：`modules/`、`references/`、`templates/`、`memory-system/`（skill 内模板）等路径，均相对于本 skill 的根目录。
   - 示例：`references/书名生成策略.md` → skill 目录下的 references/书名生成策略.md
   - 示例：`记忆系统/bible/story_bible.md`（不带 `[书名]/` 前缀）→ skill 内模板文件，只读

2. **项目文件**：带 `[书名]/` 前缀的路径，相对于当前工作目录。`[书名]` 是项目初始化时用户确认的书名。
   - 示例：`[书名]/记忆系统/bible/story_bible.md` → 项目目录下的记忆系统副本
   - 示例：`[书名]/设定/[书名]_人设卡.md` → 项目目录下的设定文件

3. **记忆系统文件区分**：
   - `memory-system/xxx`（无前缀）= skill 内的只读模板 → 仅用于项目初始化时复制
   - `[书名]/记忆系统/xxx`（有前缀）= 项目目录下的读写副本 → 所有后续操作的目标

4. **项目查找**：跨会话恢复时，在当前工作目录下搜索 `*/记忆系统/bible/story_bible.md` 定位已有项目。

# 全能小说作家

---

## ⛔ 强制执行协议

> **本技能采用模块化架构，关键内容存储在 `modules/` 目录下。** 
> 
> **技能加载后，必须先读取 `modules/00_强制执行协议.md`，严格执行其中的协议要求。**

**【首次交互必须执行】**

当用户触发本技能时，执行智能识别流程：
1. 读取 `modules/00_强制执行协议.md`（含智能启动）
2. 根据用户意图直接进入对应场景，或显示简化引导
3. 等待用户选择或提供信息

**快捷命令**：
- "继续" / "下一章" → 直接进入单章写作：
  1. 搜索 `*/记忆系统/bible/story_bible.md` 定位项目
  2. 读取项目记忆系统（bible/summaries/phases）恢复上下文
  3. 自动执行写前预检（`modules/05_24_叙事质量管理框架.md` 写前阶段），缺字段标记 ⚠️ 不阻断
  4. 加载 `modules/05_06_功能模块.md`（单章写作）
  5. 从最后一章续写 → 写后自动执行：
     【每次】圣经更新×4 + 摘要 + 五道门禁 + 强约束 + 风格自检
     【每5章】连贯性检查(17) + 质量检查(13)
     【每10章】创新检查(14) + 阶段压缩
     【每50章】卷压缩
- "书名" → 加载 `modules/05_01_功能模块.md`（项目初始化，含书名生成）
- "人设" → 加载 `modules/05_03_功能模块.md`（人物设计）
- "大纲" → 加载 `modules/05_04_功能模块.md`（情节大纲）
- "菜单" / "帮助" → 加载 `modules/04_功能模块索引.md` 显示24个功能列表
- "导出" → 加载 `modules/05_19_导出功能.md`
- "看记忆" → 加载 `modules/05_18_功能模块.md`（记忆系统查看/编辑）
- "人物跑偏了" / "人设崩了" → 加载 `modules/05_18_功能模块.md` 工作流F（紧急修复跑偏）
- "预检" → 加载 `modules/05_24_叙事质量管理框架.md`（写前预检）
- "复核" → 加载 `modules/05_24_叙事质量管理框架.md`（写后复核）
- "规则集" → 加载 `modules/05_24_叙事质量管理框架.md`（规则集管理）
- "爽点" → 加载 `modules/05_07_功能模块.md`（爽点设计）
- "战斗" / "打斗" → 加载 `modules/05_10_功能模块.md`（打斗场景）
- "诊断" / "节奏" → 加载 `modules/05_12_功能模块.md`（节奏诊断）
- "开篇" / "黄金开头" → 加载 `modules/05_05_功能模块.md`（黄金开篇）
- "批量" → 加载 `modules/05_20_批量处理.md`（批量设定）
- "金手指" / "叙事线" / "时间约束" → 加载 `modules/05_22_强约束系统.md`

---

## 📦 模块架构索引

本技能由以下模块组成，按需加载：

| 模块文件 | 内容 | 加载时机 |
|----------|------|----------|
| `modules/00_强制执行协议.md` | 执行协议（含智能启动） | 技能启动时 |
| `modules/00_智能启动协议.md` | 意图识别、快捷入口 | 技能启动时 |
| `modules/01_启动引导.md` | 场景A/B/C引导话术 | 降级时使用 |
| `modules/02_文档存储规范.md` | 文件命名、目录结构 | 创建文件时 |
| `modules/03_实时状态追踪.md` | 追踪表、连贯性检查 | 章节生成后 |
| `modules/04_功能模块索引.md` | 24个功能模块总览 | 选择功能时 |
| `modules/05_01_功能模块.md` ~ `05_18_功能模块.md` | 功能1-18详细流程 | 执行对应功能时 |
| `modules/05_19_导出功能.md` ~ `05_24_叙事质量管理框架.md` | 功能19-24详细流程 | 执行对应功能时 |
| `modules/06_场景资源映射.md` | 功能资源映射、质量检查触发、依赖链 | 执行功能时 |
| `memory-system/` | 故事圣经/人物弧线/时间线/伏笔（skill 内模板） | 项目全周期 |

---

## 📋 功能模块列表

### 核心功能（1-18）
1. 项目初始化 | 2. 世界观构建 | 3. 人物设计
4. 情节大纲 | 5. 黄金开篇 | 6. 单章写作
7. 爽点设计 | 8. 冲突反转 | 9. 对话场景
10. 打斗场景 | 11. AI/系统流 | 12. 节奏诊断
13. 质量检查 | 14. 创新检查 | 15. 商业评估
16. 修订优化 | 17. 连贯性检查 | 18. 记忆系统

### 新增功能（19-24）
19. **导出功能** - 网文/epub/Word格式导出
20. **批量设定** - 批量生成人设、支线、反派等
21. **版本控制** - 修改历史、回滚对比
22. **强约束系统** - 金手指叙事约束 / 时间约束 / 多条叙事线监控（每章自动检查）
23. **作品拆解分析** - 从已有作品中提取人物性格 / 写作风格
24. **叙事质量管理框架** - 写前预检 / 写后三阶复核 / YAML参数链条存档 / 可替换规则集

---

## 🚀 快速加载指引

### 场景 A：新开小说
```
1. 加载 modules/00_智能启动协议.md → 意图识别为「新开」
2. 提供创作模式选择（AI全自动 / 分步引导）
3. 执行功能1（项目初始化）后，自动提示规则集选择（modules/05_24_叙事质量管理框架.md 初始化阶段）
   - 用户未指定 → 默认使用「通用默认」
   - 用户指定流派 → 加载对应规则集
   - 未匹配的流派（如「科幻」「都市」「奇幻」） → 默认「通用默认」，提示可通过自定义模板创建
4. 分步模式：按序执行功能2→3→4→5（世界观→人物→大纲→开篇）
5. 全自动模式：一次性执行功能2-5，完成后进入单章写作
```

### 场景 B：继续创作
```
1. 搜索 */记忆系统/bible/story_bible.md 定位项目（找不到 → 转场景A）
2. 读取项目记忆系统（bible/summaries/phases）恢复上下文
3. 自动执行写前预检（modules/05_24_叙事质量管理框架.md 写前阶段），缺字段标 ⚠️ 不阻断
4. 加载 modules/05_06_功能模块.md（单章写作）
5. 从最后章节续写 → 章节生成完成后，自动执行写后流水线：
   
   【每次必做】
   ✓ 更新 4 个圣经文档 + 生成单章摘要
   ✓ 五道生成门禁（引用/一致性/蓝图/未知实体/描写）
   ✓ 强约束检查（金手指叙事/时间约束/叙事线监控）
   ✓ 风格与质量自检
   
   【周期触发】
   ✓ 每 5 章：连贯性检查(17) + 质量检查深度模式(13)
   ✓ 每 10 章：创新检查(14) + 阶段压缩
   ✓ 每 50 章：卷压缩
```

### 场景 C：使用单个功能
```
1. 加载 modules/04_功能模块索引.md → 展示24个功能列表
2. 用户选择功能编号 → 加载对应 modules/05_XX_xxx.md
3. 执行功能流程，完成后返回功能列表
```

### 智能识别（推荐）
```
用户输入 → 意图识别 → 直接进入对应场景

| 触发词 | 场景 | 加载模块 |
|--------|------|----------|
| 写小说 / 新开一本 | A: 新开小说 | `modules/00_智能启动协议.md` |
| 继续 / 下一章 / 接着写 | B: 继续创作 | `modules/05_06_功能模块.md` |
| 书名 / 人设 / 大纲 | C: 单个功能 | 见快捷命令 |
| 导出 / 看记忆 | C: 单个功能 | 见快捷命令 |
| 人物跑偏了 / 人设崩了 | C: 单个功能 | 功能18工作流F（紧急修复跑偏） |
| 分析《XXX》 | C: 单个功能 | `modules/05_23_作品拆解分析.md` |
| 预检 / 复核 | C: 单个功能 | `modules/05_24_叙事质量管理框架.md` |
| 规则集 | C: 单个功能 | `modules/05_24_叙事质量管理框架.md` |
| 其他触发词（爽点/战斗/诊断/金手指/批量/开篇等） | C: 单个功能 | 见快捷命令（自动映射到对应功能模块） |
```

---

## 重要说明

**记忆系统使用方式：**
- **记忆系统**：采用 LLM 原生可读/可写的自然语言文档（故事圣经/人物弧线/剧情时间线/伏笔清单）
- **结构**：`记忆系统/bible/`（4个故事圣经文档）+ `summaries/`（单章摘要）+ `phases/`（10章压缩块）
- **初始化**：项目初始化时自动创建记忆系统
- **自动维护**：每章写完后自动更新圣经 + 生成摘要
- **阶段压缩**：每10章自动将摘要压缩为梗概，确保跨会话记忆不丢失
- **跨会话恢复**：新会话读取圣经 + 压缩块即可秒回状态
- **手动管理**：功能18可查看/编辑/修复记忆系统

**强约束系统：**
- **默认启用**：每章写作后自动检查三个维度（金手指叙事/时间约束/叙事线监控）
- **叙事参谋**：不报违规，只在有风险时给出叙事建议
- **自动初始化**：约束文档不存在时自动创建，无需手动设置
- **手动触发**：`金手指` / `时间约束` / `叙事线` / `强约束`
- **存储位置**：`[书名]/记忆系统/constraints/` 下 3 个追踪文档

**叙事质量管理框架：**
- **写前预检自动触发**：「继续」/「下一章」命令在加载单章写作模块前自动执行写前状态核对
- **软阻断模式**：自动触发时缺字段仅标记 ⚠️，不中断创作流程；手动 `预检` 才严格阻断
- **规则集默认**：首次使用自动加载「通用默认」规则集，可通过 `规则集` 切换
- **存储位置**：YAML 链条随章节存档，每 10/50 章压缩至 phases/volumes/

**导出功能：**
- 网文格式：纯txt，适配各平台
- 电子书：epub格式，支持目录导航
- 投稿版：Word格式，标准投稿排版

## 🛑 操作确认点

| 类型 | 操作 | 说明 |
|------|------|------|
| **需确认** | 删除章节/设定文件 | agent 不得自主删除用户项目文件 |
| **需确认** | 覆盖记忆系统 | 覆写 bible/summaries 前必须告知 |
| **需确认** | 回滚版本 | 版本回退前展示差异，等用户确认 |
| **需确认** | 修改大纲/核心设定 | 级联影响大，展示改动摘要后确认 |
| **建议确认** | 新章节生成后 | 展示生成内容，用户点头再存档续写 |
| **建议确认** | 阶段压缩（每10章） | 摘要压缩为梗概前告知，压缩后展示 |
| **无需确认** | 单章生成 | 「继续」命令自动写入 |
| **无需确认** | 记忆系统初始化 | 首次创建项目时自动初始化 |
| **无需确认** | 强约束检查 | 每章后自动执行，仅在有风险时输出 |
| **无需确认** | 写前预检（自动模式） | 「继续」命令自动执行，缺字段标 ⚠️ 不阻断 |
| **建议确认** | 规则集中途切换 | 切换后 YAML 链条第 3 区块字段变化，在 phase 结束点切换更安全 |

## ⚠️ 边界条件

| 场景 | 处理 |
|------|------|
| 模块文件缺失 | 加载指定模块失败时，降级为直接按 SKILL.md 内置流程执行，不中断 |
| 项目目录不存在 | 跨会话恢复找不到项目 → 提示用户项目可能已移动，转为场景A |
| 多个项目匹配 | 「继续」搜到多个 bible → 列出所有项目让用户选择 |
| 用户中断创作 | 已生成的章节文件保留在 `[书名]/正文/`，下次「继续」时从最后一章接着来 |
| 规则集文件缺失 | 所选规则集 .md 文件被删除 → 降级为「通用默认」，提醒用户规则集已失效 |
| 规则集中途切换 | 切换规则集后 YAML 链条第 3 区块字段变化 → 标记切换点，建议在当前 phase 结束后再切换 |

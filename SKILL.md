---
name: 全能小说作家
description: |
  全流程长篇小说创作技能，26个功能模块，支持 AI 全自动和分步引导双模式。
  涵盖：书名生成、人设构建、大纲规划、章节写作、爽点设计、节奏诊断、
  风格创作、强约束系统、作品拆解分析、叙事质量管理。
  配备三大自动系统：自然语言记忆系统（故事圣经/人物弧线/剧情时间线/伏笔清单/角色数据库）、
  强约束系统（金手指叙事/时间约束/叙事线监控）、叙事质量管理框架（写前预检/三阶复核/参数链条）。
  
  触发词：「写小说」「新开一本」「继续」「接着写」「下一章」「书名」「人设」「大纲」
  「看记忆」「导出」「金手指」「叙事线」「分析《XXX》」「菜单」
  「预检」「复核」「规则集」
user-invocable: true
tags: [小说, 创作, 写作, 长篇, 故事, 文学, 大纲, 人物, 章节写作, 爽文, 网文, 平台文案, 书名生成, 记忆系统, 自然语言记忆, 风格创作, 短篇爆款]
version: 9.1.1
---

## 📂 文件路径约定

**本 skill 中所有文件引用遵循以下规则：**

1. **skill 内部文件**：`modules/`、`references/`、`templates/`、`memory-system/`（skill 内模板）等路径，均相对于本 skill 的根目录。
   - 示例：`references/书名生成策略.md` → skill 目录下的 references/书名生成策略.md
   - 示例：`memory-system/bible/story_bible.md`（不带 `[书名]/` 前缀）→ skill 内模板文件，只读

2. **项目文件**：带 `[书名]/` 前缀的路径，相对于当前工作目录。`[书名]` 是项目初始化时用户确认的书名。
   - 示例：`[书名]/记忆系统/bible/story_bible.md` → 项目目录下的记忆系统副本
   - 示例：`[书名]/设定/[书名]_人设卡.md` → 项目目录下的设定文件

3. **记忆系统文件区分**：
   - `memory-system/xxx`（无前缀）= skill 内的只读模板 → 仅用于项目初始化时复制
   - `[书名]/记忆系统/xxx`（有前缀）= 项目目录下的读写副本 → 所有后续操作的目标

4. **项目查找**：跨会话恢复时，在当前工作目录下搜索 `*/记忆系统/bible/story_bible.md` 定位已有项目。

# 全能小说作家

---

## ⛔ 强制执行协议

> **本技能采用模块化架构，加载后必须先读取 `modules/00_强制执行协议.md`。**
>
> 00 协议包含完整的：意图识别、场景路由（A/B/C）、写作/写后命令、快捷命令映射、写前检查点。
> **本文件不再重复这些路由逻辑，以 00 协议为唯一权威。**

---

## ⚠️ 自审悖论设计原则（★v9.0 纯调度架构）

> **核心教训**：同一个模型、同一个对话里"自己写自己审"是无效的。
> 审核结果天然偏向"还行"——因为审核器和写手共享上下文，已经被"洗脑"了。

**v9.0 纯调度架构解决方案**：

1. **三重分离**：写作（05_06 子代理）、审核（05_26 子代理×7）、修复（05_26 修复子代理）全部由独立子代理完成
2. **主代理降为编辑**：仅保留记忆维护（圣经更新+摘要）+ 调度职责。不再亲自写作、不再自检逻辑、不再读审核报告
3. **审核→修复的零补课闭环**：审核子代理返回完整报告（含段落引用+矛盾点+修改建议），修复子代理**只读审核报告+正文**，无需重读记忆文档
4. **跨会话无差异**：写作子代理起点一致（都是从磁盘读文件），消除了"主代理单次会话内的肌肉记忆优势"

**delegate_task 实操要点**：
- 子代理的 `context` 必须包含：项目工作目录路径、章节文件名、需要读取的文件列表（绝对路径）
- 子代理用 `toolsets:["file"]` 限制工具集
- 审核子代理返回完整报告（不只是 PASS/FAIL）
- 修复子代理只读审核报告+正文，不读记忆文档
- **占位符替换**：调用 delegate_task 前必须将所有 `{...}` 替换为实际值

**Pitfall**：子代理 `max_concurrent_children` 限制（默认3），不能一次起超过3个并行子代理。超过时需分批。

---

## 重要说明

**记忆系统使用方式：**
- **记忆系统**：采用 LLM 原生可读/可写的自然语言文档（故事圣经/人物弧线/剧情时间线/伏笔清单/角色数据库）
- **结构**：`记忆系统/bible/`（5个故事圣经文档）+ `summaries/`（单章摘要）+ `phases/`（10章压缩块）
- **自动维护**：每章写完后自动更新圣经 + 生成摘要
- **阶段压缩**：每10章自动将摘要压缩为梗概
- **跨会话恢复**：新会话读取圣经 + 压缩块即可秒回状态

**强约束系统：**
- **默认启用**：每章写作后自动检查三个维度（金手指叙事/时间约束/叙事线监控）
- **叙事参谋**：不报违规，只在有风险时给出叙事建议
- **存储位置**：`[书名]/记忆系统/constraints/` 下 3 个追踪文档

**叙事质量管理框架：**
- **写前预检自动触发**：「继续」/「下一章」命令在加载单章写作模块前自动执行
- **阻断与自愈**：缺字段阻断写作 → 自动回溯修复 → 重新预检通过 → 继续
- **规则集加载逻辑**：首次使用自动加载「通用默认」；缺失则降级，最终兜底为内置五维默认参数

---

## 🛑 操作确认点

| 类型 | 操作 | 说明 |
|------|------|------|
| **需确认** | 删除章节/设定文件 | agent 不得自主删除用户项目文件 |
| **需确认** | 覆盖记忆系统 | 覆写 bible/summaries 前必须告知 |
| **需确认** | 回滚版本 | 版本回退前展示差异，等用户确认 |
| **需确认** | 修改大纲/核心设定 | 级联影响大，展示改动摘要后确认 |
| **建议确认** | 新章节生成后（非连续写作模式） | 展示生成内容，用户点头再存档续写 |
| **建议确认** | 阶段压缩（每10章） | 压缩前告知，压缩后展示 |
| **无需确认** | 单章生成 | 「继续」命令自动写入 |
| **无需确认** | 记忆系统初始化 | 首次创建项目时自动初始化 |
| **无需确认** | 强约束检查 | 每章后自动执行，仅在有风险时输出 |
| **无需确认** | 写前预检（自愈模式） | 缺字段自动阻断 → 回溯修复 → 重新预检 |

---

## ⚠️ 边界条件

| 场景 | 处理 |
|------|------|
| 模块文件缺失 | 跳过该模块功能并告知用户「模块文件缺失，功能暂不可用」，不中断整体流程 |
| 记忆系统缺失 | 05_06 检测到 `[书名]/记忆系统/bible/` 目录不存在 → **直接终止**，提示先初始化记忆系统。文档不全 → 按00协议自愈修复 |
| 项目目录不存在 | 跨会话恢复找不到项目 → 提示用户项目可能已移动，转为场景A |
| 多个项目匹配 | 「继续」搜到多个 bible → 列出所有项目让用户选择 |
| 用户中断创作 | 已生成的章节文件保留，下次「继续」时从最后一章接着来 |
| 规则集文件缺失 | 降级为「通用默认」，提醒用户规则集已失效 |
| 导出无章节 | 提示「暂无章节可导出」，不生成空文件 |
| 功能依赖未满足 | 项目未初始化时触发依赖功能 → 提示「请先写小说或继续已有项目」 |
| delegate_task 不可用 | **阻断流水线**，提示用户修复配置后重试。不可降级为主代理自审 |
